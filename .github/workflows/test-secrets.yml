name: Test Secrets

on:
  workflow_dispatch:  # Manual trigger

permissions:
  secrets: read
  id-token: write
  contents: read

jobs:
  test-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Test Secret Access
        env:
          SEPOLIA_URL: ${{ secrets.SEPOLIA_URL }}
        run: |
          echo "Secrets are properly configured"
          echo "SEPOLIA_URL is set"
          # Note: We don't print the actual value to prevent exposure
